/**
 * iOS-only — not used in android-project-expo Android builds.
 * Kept for symmetry if this shell is ever prebuilt for iOS.
 */
export { runIosCallKitEntryBootstrap } from "../src/core/softphone/iosRecentsEntryBootstrap";
