/** Shared meeting UI dimensions — single source for tiles and local PiP. */
export const MEETING_TILE_BORDER_RADIUS = 12;
export const MEETING_TILE_SURFACE_BG = "#0f172a";

/** @deprecated Use MEETING_TILE_BORDER_RADIUS */
export const MEETING_PIP_BORDER_RADIUS = MEETING_TILE_BORDER_RADIUS;

export const LOCAL_PIP_WIDTH = 100;
export const LOCAL_PIP_HEIGHT = 150;
/** Extra space around PiP on Android so symmetric glow is not clipped. */
export const LOCAL_PIP_ANDROID_SHADOW_PAD = 8;
export const LOCAL_PIP_MARGIN = 16;

export const MEETING_HEADER_HEIGHT = 52;
export const MEETING_BOTTOM_CONTROLS_ESTIMATE = 112;
