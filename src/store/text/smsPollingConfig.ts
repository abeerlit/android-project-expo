/**
 * Set to false to disable SMS chat polling (for testing).
 * When false: no interval in TextChatContent and saga poll loop exits.
 * Set back to true to resume polling.
 */
export const SMS_POLLING_ENABLED = true;
